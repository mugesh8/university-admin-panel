# Used for `npm run dev` and `npm run build`. Update for production before you deploy,
# or set VITE_* variables in your hosting/CI (they override this file).

VITE_APP_URL=http://localhost:8005
VITE_API_BASE_URL=http://hub.muantigua.org/

# Production examples — swap into the lines above when going live:
# VITE_APP_URL=https://your-app.example.com
# VITE_API_BASE_URL=https://api.example.com
